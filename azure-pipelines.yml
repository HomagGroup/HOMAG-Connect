# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

# MEND
# resources:
#   repositories:
#   - repository: templates
#     type: git
#     name: OneHomag/SWC-DevOps-HoT
#     ref: main

trigger:
  branches:
    include:
    - main
    - 'dependabot/nuget/*'
  tags:
    include:
    - '*'

pr:
  branches:
    include:
      - '*'

variables:
  dotnetVersion: 10.0.x
  ProductName: HomagGroup.DigitalFactory.DigitalDevelopment
  ProjectName: HomagConnect 

  
stages:
  - stage: retrieve_version_info_from_git
    displayName: Get Version info
    jobs:
    - job: get_number_job
      timeoutInMinutes: 240
           
      steps:
      - checkout: self
        fetchDepth: 0         

      - task: PowerShell@2
        displayName: Set version environment variable
        name: version_task
        inputs:
          targetType: inline
          script: |
            git log
            $VERSION_TAG = git describe --tags --dirty --match='[0-9]*'
            echo "Set version tag: $($VERSION_TAG)"
            echo "##vso[task.setvariable variable=VERSION_TAG]$($VERSION_TAG)"      

  # MEND
  # - template: software-composition-analysis/template.yml@templates
  #   parameters:
  #     dependsOn: 
  #     - retrieve_version_info_from_git
  #     fully_qualifed_package_name: HomagGroup.DigitalFactory.Solutions.DF-Insights
  #     analyze_directory: $(Build.SourcesDirectory)
  #     package_version: $[ stageDependencies.retrieve_version_info_from_git.get_number_job.outputs['version_task.VERSION_TAG'] ]

  - stage: Build
    pool:
      #vmimage: 'windows-latest'
      name: Homag Windows Agents
    variables:
      - name: BuildConfiguration
        value: Release
      - group: dftranslationvariables
  
    # MEND
    # dependsOn: software_composition_analysis_stage
    jobs:
      - job: Build
        timeoutInMinutes: 240
           
        steps:
        - checkout: self
          fetchDepth: 0         
        
        - script: echo "Build branch is $(Build.SourceBranch)" 
          displayName: Display build branch environment variable

        - task: PowerShell@2
          displayName: Set version environment variable
          inputs:
            targetType: inline
            script: |
              git log
              $VERSION_TAG = git describe --tags --dirty --match='[0-9]*'
              echo "Set version tag: $($VERSION_TAG)"
              echo "##vso[task.setvariable variable=VERSION_TAG]$($VERSION_TAG)"

        - task: UseDotNet@2
          displayName: 'Use .NET Core sdk $(dotnetVersion)'
          inputs:
            packageType: 'sdk'
            version: $(dotnetVersion)

        - task: DotNetCoreCLI@2
          displayName: Restore packages
          inputs:
            command: 'restore'

        - script: |
            echo "VERSION_TAG is: $(VERSION_TAG)"
          displayName: Display VERSION_TAG

        - task: SonarQubePrepare@7
          displayName: 'Prepare analysis on SonarQube'
          inputs:
            SonarQube: 'SonarQube Enterprise'
            projectKey: $(ProductName).$(ProjectName)
            projectName: $(ProductName).$(ProjectName)
            projectVersion: '$(MajorMinorBuildVersion)'
            extraProperties: |
                sonar.cs.vstest.reportsPaths=$(Agent.TempDirectory)/**/*.trx
                sonar.pullrequest.key=$(System.PullRequest.PullRequestNumber)
                sonar.pullrequest.branch=$(System.PullRequest.SourceBranch)
                sonar.pullrequest.base=$(System.PullRequest.TargetBranch)
          condition: and(succeeded(), eq(variables.disableSonarQubeAnalysis, false), eq(variables['Build.Reason'], 'PullRequest'))

        - task: SonarQubePrepare@7
          displayName: 'Prepare analysis on SonarQube'
          inputs:
            SonarQube: 'SonarQube Enterprise'
            projectKey: $(ProductName).$(ProjectName)
            projectName: $(ProductName).$(ProjectName)
            projectVersion: '$(MajorMinorBuildVersion)'
            extraProperties: |
              sonar.cs.vstest.reportsPaths=$(Agent.TempDirectory)/**/*.trx
              sonar.branch.name=$(Build.SourceBranchName)
          condition: and(succeeded(), eq(variables.disableSonarQubeAnalysis, false), ne(variables['Build.Reason'], 'PullRequest'))


        - task: ResourceTranslation@3
          displayName: 'ResourceTranslation'
          inputs:
            C3POCustomFolder: true
            C3POWorkingFolder: '$(Build.SourcesDirectory)'
            RecursiveSearch: true
            C3POLanguages: $(C3PO_LANGUAGES_DIGITAL_APPS)
            AssemblyMaskList: '*.resx'
            WorkingDirectoryList: '..\s'
            C3POArguments: '-@=out.dbg -err=c3po_err.txt -s -ser -fdbjobs=all -fdbsync=incr -fdbUser=$(C3PO_TERMBASE_USER) -useenglish'
            C3PORetryCounter: '3'
            C3POTimeout: '600'
            C3PODisableCache: true
          condition:  and(succeeded(), ne(variables['Build.Reason'], 'Schedule'), or(startsWith(variables['Build.SourceBranch'], 'refs/tags/'),startsWith(variables['Build.SourceBranch'], 'refs/heads/main')))

        - task: DotNetCoreCLI@2
          displayName: Build projects
          timeoutInMinutes: 240
          inputs:
            command: 'build'            
            projects: |
                  **/*.csproj
                  !**/.git/**
                  !**/.github/**
                  !**/.gitlab/**
                  !**/logs/**
                  !**/bin/**/*.csproj
                  !**/obj/**/*.csproj
            arguments: '--configuration $(BuildConfiguration) --no-restore -nr:FALSE'

        - task: PowerShell@2
          displayName: Find projects to pack
          inputs:
            targetType: inline
            script: |
              $projectPatterns = @(
                "*.Client.csproj",
                "*.Contracts.csproj", 
                "*.Samples*.csproj",
                "HomagConnect.Base.csproj",
                "HomagConnect.Base.Contracts.csproj",
                "HomagConnect.Base.TestBase.csproj",
                "HomagConnect.DataExchange.Extensions.csproj",
                "HomagConnect.DataExchange.Contracts.csproj",
                "HomagConnect.DataExchange.Samples.csproj"
              )
              
              $excludePatterns = @(
                "*.Tests.csproj",
                "HomagConnect.Base.Tests.Api.csproj",
                "HomagConnect.MmrMobile.Samples.Console.csproj"
              )
              
              $allProjects = @()
              foreach ($pattern in $projectPatterns) {
                $found = Get-ChildItem -Path "$(Build.SourcesDirectory)" -Filter $pattern -Recurse
                $allProjects += $found
              }
              
              $projects = $allProjects | Where-Object {
                $projectFile = $_
                $shouldExclude = $false
                foreach ($excludePattern in $excludePatterns) {
                  if ($projectFile.Name -like $excludePattern) {
                    $shouldExclude = $true
                    break
                  }
                }
                -not $shouldExclude
              } | Select-Object -Unique | ForEach-Object { $_.FullName }
              
              if ($projects.Count -eq 0) {
                Write-Host "##vso[task.logissue type=error]No projects found to pack!"
                exit 1
              }
              
              Write-Host "Found $($projects.Count) projects to pack:"
              $projects | ForEach-Object { Write-Host "  - $_" }
              
              $projectsList = $projects -join "`n"
              Write-Host "##vso[task.setvariable variable=PROJECTS_TO_PACK]$projectsList"

        - task: DotNetCoreCLI@2
          displayName: Pack NuGet Packages
          inputs:
            command: 'pack'
            packagesToPack: '$(PROJECTS_TO_PACK)'
            configuration: '$(BuildConfiguration)'
            nobuild: true
            arguments: '-p:PackageVersion=$(VERSION_TAG) -p:Version=$(VERSION_TAG)'

        - publish: $(Build.ArtifactStagingDirectory)
          artifact: nuget

        - task: DotNetCoreCLI@2
          displayName: Run Integration Tests in PRD environment to ensure that the samples are working.   
          env:
            HomagConnect:BaseUrl: $(HomagConnect:BaseUrl)
            HomagConnect:SubscriptionId: $(HomagConnect:SubscriptionId)
            HomagConnect:AuthorizationKey: $(HomagConnect:AuthorizationKey)
          inputs:
            command: 'test'
            projects: '**/*.Tests.csproj'
            arguments: '--configuration $(BuildConfiguration) --no-restore --no-build --filter TestCategory!=TemporaryDisabled&TestCategory!=UserTestInteractionNeeded'                        
        
        # MEND
        # - template: common-tasks/list-agent-directory/template.yml@templates
  
        - task: SonarQubeAnalyze@7
          displayName: 'Run Code Analysis'
          condition: and(succeeded(), eq(variables.disableSonarQubeAnalysis, false))

        - task: SonarQubePublish@7
          displayName: 'Publish Quality Gate Result'
          condition: and(succeeded(), eq(variables.disableSonarQubeAnalysis, false))          

        - script: dotnet nuget push --source https://api.nuget.org/v3/index.json --api-key $(NUGET_API_KEY) "$(Build.ArtifactStagingDirectory)/*.nupkg" --skip-duplicate
          displayName: "Publish all NuGet packages on nuget.org"
          condition: and(succeeded(), or(startsWith(variables['Build.SourceBranch'], 'refs/tags/'),startsWith(variables['Build.SourceBranch'], 'refs/heads/main'))) 